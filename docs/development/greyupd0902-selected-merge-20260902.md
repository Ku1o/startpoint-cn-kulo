# greyupd0902 已确认内容合并记录（2026-09-02）

本次只合入用户逐项确认的服务端内容，采用追加/定点覆盖，不替换未确认的现有表。

## 已合入

- 45 个作者角色行（40 个本地缺少角色，以及魔王、白虎、歼灭者、深渊之兽、风巨蜥 5 个冲突角色）及其 `mana_node`、`mana_board`、服务端 CDN 角色/文本行。
- 角色魔晶板可用；没有导入 `character_awake_status`，角色觉醒仍关闭。
- 15 把深渊武器与死亡使者的服务端装备注册、属性、突破上限、强化商店和出售配置。
- 只有死亡使者 `5900101` 有分解产魂配置；15 把深渊武器没有分解产魂行。
- 客户端 CDN 保留 15 把深渊武器的分解为魂珠显示标记；服务端仍只允许死亡使者实际生成魂珠。
- 深渊武器商店统一 15 深渊代币、库存 1；深渊觉醒核商店 500 代币、库存 5；死亡使者兑换行作为独立数据保留。
- 15 把深渊武器的强化行标记为 `per_level`：每次购买按请求等级逐级推进，保留作者原有 `costs`，并在对应级段逐级追加幻想代币 ×1、属性地狱 Boss 紫币 ×5、70 级机兵齿轮 ×10、99 级机兵核心 ×2、120 级究极图腾 ×3。死亡使者保持作者独立强化链；其他原有武器行未带该标记，继续使用原有阶段福利逻辑。
- 深渊连战第 30 层随机奖励追加：深渊王印 40%（1–3 个）、深渊觉醒核 10%（1–2 个）。固定奖励、每轮奖励、原通关概率奖励未改；该随机表只在服务端最终层结算函数中调用。
- 990001 深渊池、990002 竞速池使用作者完整池名单/概率，并绑定 999013/999014、999017/999018；999015/999016 仅保留历史登记。
- 客户端 CDN 以 1.4.95 为基线接入 1.4.96 选择性表补丁（49 个有变更表成员）及作者包 33 个资源差分包；角色觉醒表不下发。
- 五重决战物品定义（凭证、图纸、深界结晶、决战之证、五王心核）只作为备用数据登记，没有启用五重决战入口、Boss 顺序或掉落。

## 明确未改

- 称号、称号商店、排行榜入口。
- APK、客户端二进制及客户端界面/图标调整由另一个会话处理；本次 CDN 只接入已确认的表和作者资源差分。
- 15 把深渊武器关卡门控；按用户安排由另一个会话处理。
- 五重决战运行时、第二批兑换和 Boss 重抽；继续使用当前固定 Boss 顺序。
