---
name: starpoint-cn-mod-tools
description: "Investigate, explain, balance, or safely patch StarPoint CN fantasy-gauntlet MOD character data and Deep Abyss content tooling with verified client/runtime evidence and the user's established presentation and authorization preferences. Use for fantasy-gauntlet-mod-tools character audits, ActionDSL edits, balance work, and abyss rerolls; do not use for awakening-only or unrelated server tasks."
---

# StarPoint CN MOD tools

Produce MOD character and Deep Abyss changes that distinguish displayed data from effective runtime semantics and preserve the user's authorization boundaries.

Before inspecting or changing StarPoint CN files, read the applicable repository `AGENTS.md`. Treat the Git source repository as authoritative and preserve all pre-existing local changes.

Classify the request before acting:

- Questions, investigations, comparisons, and diagnoses are read-only. Do not turn them into edits.
- Explicit requests to change or build authorize scoped local implementation and proportionate validation only.
- Commit, push, runtime synchronization, package creation, upload, restart, and deployment each require the authority defined by the repository instructions. Never infer one from another.

For character data investigation, reporting, balance edits, or integration handoff, read [references/character-balance-workflow.md](references/character-balance-workflow.md) and apply only the sections relevant to the request.

For ActionDSL edits or Deep Abyss reroll/build safety work, read [references/runtime-content-safety.md](references/runtime-content-safety.md). Apply its semantic gates in addition to ordinary schema and asset-existence checks.

Keep the skill adaptive but narrow. When the user confirms a correction as a reusable preference, or a demonstrated tool failure reveals a general invariant, refine the relevant rule and add a regression check when code is involved. Do not convert tentative questions, disputed interpretations, or one character's temporary balance values into permanent skill rules.
