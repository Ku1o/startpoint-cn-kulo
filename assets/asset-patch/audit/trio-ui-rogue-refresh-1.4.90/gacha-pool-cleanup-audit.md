# 1.4.89 → 1.4.90 深渊池 990001 清理审计

- 本项与 `pinball-1.4.89-1.4.90-1-0827-trio-ui-refresh.zip` 协调发布，不新增版本边。
- 客户端 ZIP 保持 97 个哈希资源成员；`assets/gacha.json` 与 `assets/gacha_cnmod.json` 是服务端运行时文件，不嵌入客户端 ZIP。
- 两份服务端文件的 `990001` 完全一致，且后加载的 `gacha_cnmod.json` 已作为实际胜出来源校验。
- 保留审核指定的 `123001`、`131182`、`213007`、`263003`、`263009`、`263015`，仅移除其余 26 名非扭蛋角色。
- 池规模由 `253/144/78` 调整为 `248/125/76`；五星、四星、三星总权重仍为 `1593000/2184/1113`。
- `rankRates` 不变；3 名高 UP 仍各 `0.38%`，其余 14 名 UP 仍各 `0.10%`，对应对象行和权重保持不变。
- 移除权重只分摊给同星级非 UP 留存角色。
- MOD 工具编译合同加入 26 名排除项，防止将来从 donor 池重新引入；6 名审核例外不在排除项中。
- 当前仅完成静态、加载层与概率回归；未同步运行镜像，未做真机抽卡验证。
